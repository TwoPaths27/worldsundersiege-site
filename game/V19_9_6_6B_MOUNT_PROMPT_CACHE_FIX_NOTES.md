# V19.9.6.6b — Mount Prompt Cache Fix

- Updated the `game.css` and `battlefield.js` cache versions in `index.html`.
- Forces the direct `MOUNT?` control above the Mount token.
- Keeps the rider selected when the legal Mount tile is clicked.
- This patch changes only the Mount prompt presentation/loading path.
